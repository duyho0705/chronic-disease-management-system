services:
  redis:
    image: redis:7-alpine
    container_name: cdm-redis
    ports:
      - "6379:6379"
    volumes:
      - cdm_redis_data:/data
    command: redis-server --save 60 1 --loglevel warning

volumes:
  cdm_redis_data:
